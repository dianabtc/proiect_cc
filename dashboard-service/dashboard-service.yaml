apiVersion: v1
kind: Service
metadata:
  name: portainer
  namespace: kubernetes-dashboard
  labels:
    app: portainer
spec:
  type: NodePort
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
      nodePort: 30900
  selector:
    app: portainer
