apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  type: {{ .Values.auth.service.type }}
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: {{ .Values.auth.service.port }}
      targetPort: 8000
