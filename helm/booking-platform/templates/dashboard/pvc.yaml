apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.dashboard.pvc.name }}
  namespace: kubernetes-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.dashboard.pvc.size }}